
1. The way Pillow is installed is too crude; the installation should be done by invoking the command line.
2. Fix the bug where, after a material's texture file has been changed, the texture size cannot be read and the texture therefore cannot be merged.
